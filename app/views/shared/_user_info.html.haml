.user_info
  %a{:href => "#{user_path(@user)}" }
    = gravatar_for @user, :size => 30
    %span.user_name
      = @user.name
    %span.microposts
      = pluralize(@user.microposts.count, "micropost")

